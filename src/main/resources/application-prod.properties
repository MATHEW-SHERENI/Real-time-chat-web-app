# Production profile — used automatically inside the Docker container
# PORT is set by cloud hosts like Render; defaults to 8080 locally
server.port=${PORT:8080}

# Health endpoint for monitoring (used later on Render)
management.endpoints.web.exposure.include=health
management.endpoint.health.probes.enabled=true
